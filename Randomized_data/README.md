#  Randomized_data

Randomly generated information is more authentic, which is less likely to be guessed as auto-program by Bunnings website, thus reducing the likelihood being taken further action.

The server speed of Bunnings is slow, so it needs to wait for each input field of the form to fully load before doing anything.

Calling driver.quit(); may interfere with visual check.

